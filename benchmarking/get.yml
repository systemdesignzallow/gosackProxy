config:
  target: "http://localhost:3000"
  processor: "./benchmarkingHelpers.js"
  phases:
    - duration: 60
      arrivalRate: 1500
scenarios:
  - flow:
      - function: "getRandomInt"
      - get:
          url: "/houses/{{ number }}"
