config:
  target: 'http://localhost:3000'
  processor: './benchmarkingHelpers.js'
  phases:
    - duration: 60
      arrivalRate: 1000
scenarios:
  - flow:
      - function: 'getRandomInt'
      - put:
          url: '/houses/{{ number }}'
