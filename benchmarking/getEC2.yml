config:
  target: "http://ec2-34-219-58-35.us-west-2.compute.amazonaws.com"
  processor: "./benchmarkingHelpers.js"
  phases:
    - duration: 60
      arrivalRate: 250
scenarios:
  - flow:
      - function: "getRandomInt"
      - get:
          url: "/houses/{{ number }}"
